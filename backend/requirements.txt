# Core Flask Dependencies
Flask==2.3.3
Flask-CORS==4.0.0
Werkzeug==2.3.7

# PDF Processing (lightweight)
PyMuPDF==1.23.8
python-docx==1.1.0
Pillow==10.2.0

# OCR (basic - Tesseract)
pytesseract==0.3.10

# Utilities
python-dotenv==1.0.0
python-dateutil==2.8.2
requests==2.31.0

# Azure Services
azure-functions==1.18.0
azure-cosmos==4.5.1

# Database (optional - for MySQL fallback)
mysql-connector-python==8.0.33

# AI Features are OPTIONAL - Install separately with:
# pip install -r requirements-ai.txt
# These include: sentence-transformers, faiss-cpu, torch (3+ GB)
