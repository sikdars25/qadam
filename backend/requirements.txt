# Core Flask Dependencies
Flask==2.3.3
Flask-CORS==4.0.0
Werkzeug==2.3.7

# PDF Processing (lightweight)
PyMuPDF==1.23.8
python-docx==1.1.0
Pillow==10.2.0

# OCR - Moved to separate Function App (./ocr/)
# pytesseract==0.3.10  # No longer needed - using PaddleOCR in separate service

# Utilities
python-dotenv==1.0.0
python-dateutil==2.8.2
requests==2.31.0

# Azure Services
azure-functions==1.18.0
azure-cosmos==4.5.0
azure-storage-blob==12.19.0
# Pin cryptography to version compatible with Azure Functions GLIBC
cryptography==41.0.7

# Database (optional - for MySQL fallback)
mysql-connector-python==8.0.33

# AI Features (Lightweight - ~100 MB total)
# OCR: Tesseract (already included above)
# Text Generation: Groq API
groq==0.11.0
# Embeddings & Vector Search: scikit-learn for TF-IDF
scikit-learn==1.3.2
numpy==1.24.3
scipy==1.10.1
# NLP Processing
nltk==3.8.1
