# Lightweight AI Stack for Azure Functions Free Tier
# Total size: ~200 MB (fits within Azure limits)

# OCR - Tesseract (already in requirements.txt)
pytesseract==0.3.10

# PDF Processing (already in requirements.txt)
PyMuPDF==1.23.8
python-docx==1.1.0
Pillow==10.2.0

# Lightweight NLP - Use OpenAI/Groq API instead of local models
openai==1.3.0  # For OpenAI API
groq==0.4.1    # For Groq API (faster, free tier available)

# Text Processing
nltk==3.8.1    # Lightweight NLP toolkit (~50 MB)
scikit-learn==1.3.2  # For TF-IDF vectorization (~40 MB)

# Vector Search - Lightweight alternative to FAISS
numpy==1.24.3  # For basic vector operations
scipy==1.11.4  # For cosine similarity

# Optional: Hugging Face Transformers (only if needed)
# transformers==4.35.0  # ~400 MB - only install if using specific models
