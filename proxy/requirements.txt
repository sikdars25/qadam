# Core Flask Dependencies
Flask==2.3.3
Flask-CORS==4.0.0
Werkzeug==2.3.7

# PDF Processing (lightweight)
PyMuPDF==1.23.8
python-docx==1.1.0
Pillow==10.2.0

# OCR - Moved to separate Function App (./ocr/)
# pytesseract==0.3.10  # No longer needed - using PaddleOCR in separate service

# Utilities
python-dotenv==1.0.0
python-dateutil==2.8.2
requests==2.31.0

# Azure Services
azure-functions==1.18.0
azure-cosmos==4.5.0
azure-storage-blob==12.19.0
# Pin cryptography to version compatible with Azure Functions GLIBC
cryptography==41.0.7

# Database (optional - for MySQL fallback)
mysql-connector-python==8.0.33

# AI Features - Now handled by VM service
# AI client for calling VM service (no heavy dependencies needed)
# Removed: groq, scikit-learn, numpy, scipy, nltk
# These are now in the AI VM service (backend-ai/ai/)
