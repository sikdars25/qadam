# OCR Service - Flask App on Azure VM
# Using EasyOCR instead of PaddleOCR

# Flask
Flask==3.0.0
flask-cors==4.0.0

# EasyOCR (lightweight alternative to PaddleOCR)
easyocr==1.7.0

# Image processing - CRITICAL: Use NumPy 1.x for compatibility
Pillow>=10.0.0
numpy<2.0.0
opencv-python-headless==4.8.1.78

# PDF Processing (for extracting images from PDFs)
PyMuPDF>=1.23.0

# Production server
gunicorn==21.2.0

# Note: EasyOCR advantages:
# - Simpler installation than PaddleOCR
# - Better maintained
# - Good accuracy for printed text
# - 80+ languages support
# - No GPU required (CPU mode works well)
